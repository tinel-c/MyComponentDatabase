# Default: SQLite (no Docker). For PostgreSQL, set provider in prisma/schema.prisma and use:
# DATABASE_URL="postgresql://warehouse:warehouse@localhost:5432/warehouse?schema=public"
DATABASE_URL="file:./dev.db"
